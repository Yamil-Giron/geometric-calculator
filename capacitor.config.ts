import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'geometric.calculator',
  appName: 'geometric-calculator',
  webDir: 'www'
};

export default config;
